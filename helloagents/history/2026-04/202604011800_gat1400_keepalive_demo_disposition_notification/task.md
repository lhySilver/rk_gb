# 任务清单

- [√] 将 keepalive demo 的发送入口改为 `PostDispositionNotifications()`
- [√] 使用 `PersonObject + SubImage(Data)` 组最小告警通知
- [√] 保留本地图片 `/mnt/sdcard/test.jpeg` 与最多 `5` 次的触发约束
- [√] 同步更新 `gat1400` 模块文档、`CHANGELOG` 和历史索引
