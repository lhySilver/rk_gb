#ifndef __ENUM_DISKU__
#define __ENUM_DISKU__

#include <string>
#include <string.h>
using namespace std;
bool FindDiskU(string& strPath, string& strID);
void RepairDiskU(const string& strPath, const string& strID);

#endif
